# libtersafe.so SO 注入检测机制全面分析

> **分析目标**: libtersafe.so (腾讯 TSS 安全 SDK) — 检测 SO 库是否被注入的核心函数  
> **架构**: ARM64 (AArch64) — 64位 ARM 处理器架构  
> **文件**: libtersafe.so (MD5: a098ce6b20e93cc52aa0c053d68c6af4)  
> **IDA 数据库**: libtersafe.so.i64  
> **分析日期**: 2026-08-05

---

## 📖 新手入门指南

### 什么是 SO 注入？

**SO**（Shared Object，共享对象）是 Android/Linux 中的动态链接库文件（类似 Windows 的 DLL）。**SO 注入**是指恶意程序强制将一个 SO 库加载到目标进程的内存空间中，从而窃取数据、修改程序行为或绕过安全检测。

例如，游戏外挂常常通过 SO 注入将作弊代码注入到游戏进程中。

### 什么是 TSS？

**TSS**（Tencent Safe System，腾讯安全系统）是腾讯开发的一款反作弊/安全 SDK，用于保护游戏和应用免受外挂、调试器、注入等攻击。`libtersafe.so` 是其核心库。

### 本分析的目标

找出 `libtersafe.so` 中**检测是否有外部 SO 库被注入到当前进程**的核心函数，并理解其工作原理。

---

## 目录

1. [概述](#1-概述)
2. [调用链分析](#2-调用链分析)
3. [核心函数 sub_5299E8 详细分析](#3-核心函数-sub_5299e8-详细分析)
4. [dl_iterate_phdr 回调 sub_52C38C](#4-dl_iterate_phdr-回调-sub_52c38c)
5. [.eh_frame_hdr 验证 sub_52C4C0](#5-eh_frame_hdr-验证-sub_52c4c0)
6. [地址范围缓存数据库](#6-地址范围缓存数据库)
7. [process_vm_readv 后备检测](#7-process_vm_readv-后备检测)
8. [辅助函数分析](#8-辅助函数分析)
9. [检测流程图](#9-检测流程图)
10. [绕过思路分析](#10-绕过思路分析)
11. [全局数据与关键常量](#11-全局数据与关键常量)

---

## 1. 概述

`libtersafe.so` 使用**三种互补技术**来检测 SO 注入，像三层过滤网一样层层筛查：

| 检测技术 | 通俗解释 | 优先级 |
|---------|---------|--------|
| `dl_iterate_phdr` 枚举 | 📋 **查户口**：遍历所有已加载的库，看每个库是否"证件齐全" | 最高 |
| 地址范围缓存数据库 | 🗄️ **查档案**：在已知的"白名单"地址范围内查找 | 中 |
| `process_vm_readv` 系统调用 | 🔍 **验指纹**：直接读取内存中的指令，验证是否是合法代码 | 后备 |

> **💡 专业术语说明**:
> - **dl_iterate_phdr**: 一个系统函数，可以遍历当前进程加载的所有动态库，并获取每个库的信息（如基址、大小、程序头等）
> - **process_vm_readv**: 一个系统调用（syscall），允许一个进程读取另一个进程的内存（这里用于读取自身内存）
> - **系统调用 (syscall)**: 用户态程序请求内核执行特权操作的接口

### 1.1 关键函数索引

| 函数名 (IDA 自动命名) | 地址 (十六进制) | 大小 (字节) | 作用（通俗解释） |
|----------------------|---------------|------------|----------------|
| `sub_528F20` | 0x528F20 | 168 | **准备工作**：初始化检测所需的数据结构 |
| `sub_5299E8` | 0x5299E8 | 640 | **★ 核心检测函数**：三道检测关卡的入口 |
| `sub_52C38C` | 0x52C38C | 308 | **查户口员**：检查每个已加载的 SO 是否合法 |
| `sub_52C0B8` | 0x52C0B8 | 724 | **档案管理员**：在地址缓存中查找并验证 |
| `sub_52C4C0` | 0x52C4C0 | 288 | **证件检验员**：解析 SO 的异常处理框架头 |
| `sub_52A21C` | 0x52A21C | 3144 | **解码器**：解码 DWARF 格式的异常处理信息 |
| `sub_5291C0` | 0x5291C0 | 400 | **解密员**：解码/去混淆缓存中的数据 |
| `sub_529044` | 0x529044 | 236 | **定位器**：在流中定位到指定位置 |

> **💡 IDA 命名规则**: `sub_XXXXXX` 表示 IDA 无法自动识别函数名，使用地址作为名称。`0x` 前缀表示十六进制数。

---

## 2. 调用链分析

### 2.1 什么是调用链？

**调用链**（Call Chain）是指一个函数调用另一个函数，再调用另一个函数...形成的链条。就像公司里的"上级→下级"关系。

### 2.2 完整调用链

```
[JNI/API 入口] ← 游戏或其他应用调用 TSS SDK 的入口
      │
      ▼
sub_528AB0 / sub_528BA8 / sub_528D60    ← 三个上层调用者（不同场景的入口）
      │
      ▼
sub_528F20                              ← 准备工作：初始化上下文结构
      │
      ▼
sub_5299E8                              ← ★ 核心检测函数（三道关卡的总入口）
      │
      ├──▶ sub_52C38C                   ← 关卡一：dl_iterate_phdr 回调
      │       │                                （遍历所有已加载的 SO）
      │       └──▶ sub_52C4C0           ← 验证 SO 的 .eh_frame_hdr 是否合法
      │
      ├──▶ sub_52C0B8                   ← 关卡二：地址范围验证
      │       │                                （在白名单缓存中查找）
      │       ├──▶ sub_52C5E0           ← 二进制数据比较
      │       ├──▶ sub_52C7A8           ← 模式匹配
      │       └──▶ sub_52A21C           ← DWARF CFI 解码（解析异常处理信息）
      │
      ├──▶ sub_5291C0                   ← 数据解码（解密缓存中的编码数据）
      │
      └──▶ syscall(270, ...)            ← 关卡三：process_vm_readv 后备检测
                                                   （直接读取内存验证指令）
```

### 2.3 上层调用者（初始化函数）

三个上层调用者 (`sub_528AB0`, `sub_528BA8`, `sub_528D60`) 都调用 `sub_528F20` 来初始化检测上下文。

> **💡 什么是上下文 (Context)?**  
> 上下文就像一个"工作文件夹"，里面存放了检测过程中需要的所有数据，包括输入参数、中间结果、最终结果等。函数之间通过传递上下文指针来共享数据。

```c
// sub_528F20 (地址: 0x528F20)
// 作用: 初始化检测上下文，然后调用核心检测函数
__int64 __fastcall sub_528F20(__int64 a1, _OWORD *a2)
{
    // ★ 关键点: 设置 vtable（虚函数表）
    // 虚函数表是一个函数指针数组，通过它可以调用不同的"方法"
    // 类似面向对象编程中的多态
    *(_QWORD *)a1 = off_53AA20;           // vtable (地址: 0x53AA20)
    *(_QWORD *)(a1 + 8) = &unk_59CEF8;    // 内部数据对象指针
    
    // 复制 0x110 (272) 字节的输入数据到上下文
    // 这些数据包含了需要检测的代码信息
    memcpy((void *)(a1 + 16), a2, 0x110uLL);
    
    // ... 复制更多字段到上下文中 ...
    
    // 清零结果缓冲区 (偏移 0x220 开始)
    // 在检测开始前先清空上一次的结果
    *(_OWORD *)(a1 + 544) = 0u;
    *(_OWORD *)(a1 + 560) = 0u;
    *(_OWORD *)(a1 + 576) = 0u;
    *(_OWORD *)(a1 + 592) = 0u;
    
    // ★ 调用核心检测函数
    sub_5299E8(a1, 0);    // 参数: a1=上下文, 0=标志位
    return 0LL;
}
```

**关键数据**:
- `off_53AA20` (0x53AA20): vtable 函数表指针 — 类似一个"工具箱"，里面放着各种工具函数
- `unk_59CEF8` (0x59CEF8): 内部数据对象 — "unk" 表示 IDA 未知其类型
- 上下文结构体大小: 约 0x26C 字节 (618 字节)

---

## 3. 核心函数 sub_5299E8 详细分析

### 3.1 函数原型

```c
__int64 __fastcall sub_5299E8(__int64 a1, char a2)
```

**参数**:
- `a1`: 上下文结构体指针 (由 `sub_528F20` 初始化)
- `a2`: 标志位 (bit 0 用于地址对齐修正，通常为 0)

**地址**: 0x5299E8  
**大小**: 0x280 字节 (640 字节)  
**复杂度**: 21 个基本块, 循环复杂度 11

> **💡 什么是基本块和循环复杂度?**
> - **基本块**: 代码中一段连续的、没有分支的指令序列
> - **循环复杂度**: 衡量代码复杂程度的指标，值越高表示代码越复杂、分支越多

### 3.2 完整伪代码（含详细注释）

> **💡 什么是伪代码?**  
> 伪代码（Pseudocode）是 IDA Pro 的 Hex-Rays 反编译器将汇编代码转换成的类 C 语言代码，比汇编更容易阅读。虽然不是 100% 精确，但能很好地反映程序的逻辑。

```c
__int64 __fastcall sub_5299E8(__int64 a1, char a2)
{
    // === 变量声明 ===
    __int64 v2;              // 临时变量
    __int64 result;          // 返回值
    unsigned __int64 v6;     // v20 - 目标地址（需要检测的代码地址）
    __int64 *i;              // x8 - 缓存遍历指针
    unsigned int *v8;        // x21 - 编码数据指针
    __int64 v9, v10;         // 临时变量
    __int128 v11;            // 128位寄存器数据
    __int64 v12, v13;        // 临时变量
    __int64 v14;             // v20 - 读取到的地址
    unsigned int v15;        // w0 - 进程ID (PID)
    _QWORD v16[7];           // 栈上缓冲区（7个64位值）
    __int64 v17;             // 读取结果缓冲区
    __int64 v18;             // 临时变量
    __int128 v19;            // 128位数据
    __int64 v20;             // 地址结果
    __int64 v21[6];          // dl_iterate_phdr 的输出结果
    __int64 *data;           // 数据指针
    __int64 *v23;            // 辅助指针
    unsigned __int64 v24;    // 对齐后的地址

    // ==========================================
    // 第一步: 清零"已检测"标志
    // 偏移 0x26A (= 618) 处的字节 → 检测完成标志
    // 1 = 已经检测过且合法，0 = 未检测或检测到问题
    // ==========================================
    *(_BYTE *)(a1 + 618) = 0;

    // ==========================================
    // 第二步: 获取当前代码地址 (PC)
    // 通过 vtable 中的第 4 个函数 (偏移 24 = 0x18)
    // 传入参数 0xFFFFFFFF (-1) 表示"获取当前地址"
    // ==========================================
    result = (*(__int64 (__fastcall **)(__int64, __int64))
              (*(_QWORD *)a1 + 24))(a1, 0xFFFFFFFFLL);
    
    if (result)  // 如果成功获取到地址
    {
        // 第三步: 对齐地址（根据标志位修正）
        // 如果 a2 的 bit 0 = 1，则地址减 1 对齐
        v6 = result - (a2 & 1);

        // ==========================================
        // ★ 第四步: 关卡一 — 使用 dl_iterate_phdr
        // 遍历所有已加载的 SO，检查目标地址所在的 SO 是否合法
        // ==========================================
        data = *(__int64 **)(a1 + 8);      // 获取内部数据指针
        v23 = v21;                          // 输出结果数组
        v24 = v6;                           // 保存对齐后的地址
        
        // dl_iterate_phdr 会遍历每一个已加载的共享库
        // 对每个库调用回调函数 sub_52C38C
        if (!dl_iterate_phdr(
                (int (*)(struct dl_phdr_info *, size_t, void *))sub_52C38C,
                &data)
            || !v21[2]              // v21[2] 表示回调是否找到匹配
            || (result = sub_52C0B8(a1, v6, v21, 0), (result & 1) == 0))
            // 如果上面的条件成立（三种情况任一）：
            // 1. dl_iterate_phdr 返回 0（没找到匹配）
            // 2. 回调没有找到匹配的 SO
            // 3. 验证函数 sub_52C0B8 验证失败
            // 则说明关卡一没有通过，进入关卡二
        {
            // ==========================================
            // ★ 第五步: 关卡二 — 查询地址范围缓存数据库
            // 在全局的"白名单"地址范围中查找
            // ==========================================
            
            // 加读锁（防止其他线程同时修改缓存）
            pthread_rwlock_rdlock(&stru_59CEFC);

            // 遍历全局地址范围缓存
            // off_583328[0] 指向缓存数组的起始
            // off_583330 指向缓存数组的结束
            for (i = off_583328[0]; ; i += 4)  // 每个条目占 4 个 64 位值 = 32 字节
            {
                if (i >= off_583330)           // 遍历完所有条目
                {
                    pthread_rwlock_unlock(&stru_59CEFC);  // 解锁
                    goto LABEL_15;             // 未找到匹配，进入关卡三
                }
                
                // 检查地址是否在缓存条目的范围内
                // 每个缓存条目的结构:
                //   i[0] = 模块 ID
                //   i[1] = 范围起始地址
                //   i[2] = 范围结束地址
                //   i[3] = 数据指针
                if (i[1] <= v6 && i[2] > v6)
                    break;                      // 找到匹配！
            }

            v8 = (unsigned int *)i[3];          // 获取编码数据指针
            pthread_rwlock_unlock(&stru_59CEFC); // 解锁

            if (v8)  // 如果有数据
            {
                // 第六步: 解码缓存中的数据
                // sub_5291C0 负责解码/去混淆
                if (!sub_5291C0(*(_QWORD *)(a1 + 8), v8, &v17, (__int64)v16, 0))
                {
                    // 第七步: 执行 DWARF CFI 解码
                    // sub_52A21C 是一个完整的 DWARF 异常处理信息解码器
                    memset(&data, 0, 0x618uLL);  // 清空缓冲区
                    result = sub_52A21C(
                        *(_QWORD *)(a1 + 8), &v17, (__int64)v16, v6, 4, &data);
                    if ((result & 1) != 0)       // 解码成功
                    {
                        // 填充结果结构体并返回
                        // 这些结果包括解码后的地址、大小、类型等信息
                        ...
                        return result;
                    }
                }
            }

LABEL_15:
            // ==========================================
            // ★ 第八步: 关卡三 — process_vm_readv 后备检测
            // 当关卡一和关卡二都失败时，使用最后的"杀手锏"
            // ==========================================

            // 获取目标地址
            v14 = (*(__int64 (__fastcall **)(__int64, __int64))
                   (*(_QWORD *)a1 + 24LL))(a1, 0xFFFFFFFFLL);

            // 准备 iovec 结构（用于跨进程内存读取）
            data = &v17;                    // 本地缓冲区地址
            v23 = (_QWORD *)&byte_8;        // 读取大小 = 8 字节
            v16[0] = v14;                   // 远程地址（要读取的目标地址）
            v16[1] = 8LL;                   // 读取 8 字节

            // 获取当前进程 ID
            v15 = getpid();

            // ★ 执行 process_vm_readv 系统调用
            // syscall 编号 270 (0x10E) = process_vm_readv (ARM64 Linux)
            // 作用: 读取指定进程的内存数据
            result = syscall(270LL, v15, &data, 1LL, v16, 1LL, 0LL);

            // ==========================================
            // 第九步: 验证读取到的指令模式
            // 检查目标地址的代码是否为合法的 ARM64 指令
            // ==========================================
            if (result == 8 && v17 == 0xD4000001D2801168LL)
            {
                // ✅ 匹配成功！这是合法代码
                // 0xD2801168 = MOV X8, #0x8B (ARM64 指令)
                // 0xD4000001 = SVC #0x0      (系统调用指令)
                // 这是典型的 ARM64 系统调用序列
                
                *(_QWORD *)(a1 + 608) = 0LL;           // 数据地址 = 0
                *(_QWORD *)(a1 + 552) = v14 + 4;       // 结束地址 = 地址 + 4
                *(_QWORD *)(a1 + 544) = v14;            // 起始地址
                *(_BYTE *)(a1 + 618) = 1;               // 设置"已检测"标志
                *(_OWORD *)(a1 + 576) = 0u;             // 清零其他字段
                *(_OWORD *)(a1 + 592) = 0u;
                *(_OWORD *)(a1 + 560) = 0u;
            }
            else
            {
                // ❌ 不匹配 → 检测到注入！
                // 目标地址的代码不是预期的系统调用序列
                // 说明可能有外部的 SO 被注入到了这个地址
                *(_BYTE *)(a1 + 616) = 1;   // 设置"注入检测"标志
            }
        }
    }
    else
    {
        // 无法获取地址 → 也标记为检测到注入
        // 这种情况通常不会发生，如果发生了说明有问题
        *(_BYTE *)(a1 + 616) = 1;
    }
    return result;
}
```

### 3.3 结果结构体详解

上下文结构体中的偏移用于存储检测结果：

| 偏移 (十六进制) | 偏移 (十进制) | 大小 | 名称 | 通俗解释 |
|----------------|--------------|------|------|---------|
| +0x220 | 544 | 8 字节 | 起始地址 | 检测到的代码起始地址 |
| +0x228 | 552 | 8 字节 | 结束地址 | 检测到的代码结束地址 |
| +0x230 | 560 | 8 字节 | 数据指针 | 指向相关数据的指针 |
| +0x238 | 568 | 8 字节 | 大小 | 代码段的大小 |
| +0x240 | 576 | 8 字节 | 对齐地址 | 对齐后的实际地址 |
| +0x248 | 584 | 8 字节 | 预留 | 保留字段，暂未使用 |
| +0x250 | 592 | 4 字节 | 标志 | 标志位 (0x3000000) |
| +0x254 | 596 | 4 字节 | 类型 | 代码类型 |
| +0x258 | 600 | 8 字节 | 代码地址 | 可执行代码的地址 |
| +0x260 | 608 | 8 字节 | 数据地址 | 相关数据的地址 |
| **+0x268** | **616** | **1 字节** | **注入检测标志** | **★ 1 = 检测到注入！** |
| **+0x26A** | **618** | **1 字节** | **检测完成标志** | **★ 1 = 检测完成，代码合法** |

> **💡 检测逻辑总结**:
> - `a1+0x268 = 1` → **发现注入！** 目标地址的代码不是合法的 SO 代码
> - `a1+0x26A = 1` → **检测通过** 目标地址的代码是合法的

### 3.4 汇编关键点

> **💡 什么是汇编?**  
> 汇编 (Assembly) 是 CPU 直接执行的机器指令的文本表示。ARM64 是 64 位 ARM 处理器使用的指令集。

```asm
; 函数入口
5299e8  PACIASP                          ; ARMv8.3 Pointer Authentication 保护
                                          ; 一种安全特性，防止代码被篡改
5299ec  STP X29, X30, [SP,#-0x30+var_s0]!  ; 保存寄存器到栈
...
5299fc  SUB SP, SP, #0x6B0               ; 在栈上分配 0x6B0 (1712) 字节空间
                                          ; 相当于 C 语言的局部变量

; dl_iterate_phdr 调用
529a34  ADR X0, sub_52C38C              ; X0 = 回调函数地址
529a38  ADD X1, SP, #...data             ; X1 = 用户数据指针
529a44  BL .dl_iterate_phdr               ; 调用 dl_iterate_phdr

; process_vm_readv 系统调用
529ba8  MOV W0, #0x10E                   ; W0 = 270 = 系统调用号 (process_vm_readv)
529bac  MOV W3, #1                        ; flags = 1
529bb0  MOV W5, #1
529bb4  MOV W6, WZR                       ; W6 = 0
529bbc  BL .syscall                       ; 执行系统调用

; 验证读取结果
529bc0  CMP X0, #8                        ; 检查是否成功读取了 8 字节
529bc8  LDR W8, [SP,#...]                 ; 读取低 32 位
529bcc  MOV W9, #0xD2801168               ; 预期的低 32 位值
529bd4  CMP W8, W9                        ; 比较
529bdc  LDR W8, [SP,#... + 4]             ; 读取高 32 位
529be0  MOV W9, #0xD4000001               ; 预期的高 32 位值
529be8  CMP W8, W9                        ; 比较
```

---

## 4. dl_iterate_phdr 回调 sub_52C38C

### 4.1 什么是 dl_iterate_phdr？

`dl_iterate_phdr` 是 Linux 系统提供的一个函数，它的作用是**遍历当前进程加载的所有动态库**。每找到一个库，就调用一次回调函数，并传入该库的信息。

> **💡 类比**: 就像快递员按门牌号挨家挨户送快递，每敲开一扇门就记录一下这家的信息。

### 4.2 函数原型

```c
__int64 __fastcall sub_52C38C(
    unsigned __int64 *a1,    // dl_phdr_info 结构体 — 包含当前 SO 的信息
    __int64 a2,              // size — 结构体大小（由系统传入）
    __int64 *a3)             // 用户数据 — 我们传入的上下文和输出缓冲区
```

**地址**: 0x52C38C  
**大小**: 0x134 字节 (308 字节)

### 4.3 什么是 dl_phdr_info？

`dl_phdr_info` 是系统定义的结构体，包含一个已加载 SO 的完整信息：

```c
struct dl_phdr_info {
    Elf64_Addr  dlpi_addr;    // SO 的基址（加载到内存中的起始地址）
    const char *dlpi_name;    // SO 的文件名（如 "libc.so"）
    const Elf64_Phdr *dlpi_phdr;  // 程序头表指针
    Elf64_Half  dlpi_phnum;   // 程序头数量
    // ... 更多字段
};
```

> **💡 什么是程序头表 (Program Header Table)?**  
> 每个 ELF 文件（SO 文件）都有一个程序头表，描述了文件如何加载到内存中。每个程序头描述了一个"段"（Segment），如代码段、数据段等。

### 4.4 伪代码（含详细注释）

```c
__int64 __fastcall sub_52C38C(unsigned __int64 *a1, __int64 a2, __int64 *a3)
{
    // a1 = dl_phdr_info 结构体指针
    // a1[0]  = dlpi_addr (SO 基址)
    // a1[2]  = dlpi_phdr (程序头表指针)
    // *(uint16*)(a1+12) = dlpi_phnum (程序头数量)

    // 第一步: 获取程序头的数量
    int v3 = *((unsigned __int16 *)a1 + 12);  // phnum
    if (!v3) return 0;                         // 没有程序头 → 跳过这个 SO

    // 第二步: 获取我们要查找的目标地址
    unsigned __int64 v4 = a3[2];               // 目标地址
    unsigned __int64 v5 = *a1;                 // 当前 SO 的基址

    if (v4 < v5) return 0;                     // 目标地址小于基址 → 不在这个 SO 中

    // 第三步: 遍历程序头表，查找 PT_LOAD 段
    // PT_LOAD (type = 1) 表示需要加载到内存的段
    _DWORD *v9 = (_DWORD *)a1[2];              // phdr 表指针
    for (int i = 0; i < v3; i++)
    {
        if (v9[i * 14] == 1)                   // 检查是否是 PT_LOAD 段
        {
            // 计算这个段在内存中的范围
            unsigned __int64 seg_start = v9[i * 14 + 2] + v5;  // p_vaddr + 基址 = 段起始地址
            unsigned __int64 seg_size = v9[i * 14 + 5];         // p_memsz = 段内存大小
            unsigned __int64 seg_end = seg_start + seg_size;    // 段结束地址

            // 检查目标地址是否落在这个段内
            if (v4 >= seg_start && v4 < seg_end)
            {
                // ✅ 找到了！目标地址在这个 SO 的某个段中
                *a3[1] = seg_start;     // 保存段起始地址
                a3[1][1] = seg_size;    // 保存段大小

                // 第四步: 查找 PT_GNU_EH_FRAME 段
                // 这个段包含异常处理框架信息，是合法 SO 必须有的
                // 段类型常量: 0x6474E550 = 1685382480
                // 注意: 在正常的 ELF 中 PT_GNU_EH_FRAME = 0x6474E550
                for (int j = v3 - 1; j >= 0; j--)
                {
                    if (*(_DWORD *)(a1[2] + 56 * j) == 0x6474E550)
                    {
                        // 找到 PT_GNU_EH_FRAME 段
                        void *note_addr = *(void **)(a1[2] + 56 * j + 16);  // p_vaddr
                        size_t note_size = *(_QWORD *)(a1[2] + 56 * j + 40); // p_memsz
                        note_addr += v5;  // 转换为绝对地址

                        // 保存到结果
                        a3[1][4] = note_addr;   // 段地址
                        a3[1][5] = note_size;   // 段大小

                        // ★ 第五步: 验证 .eh_frame_hdr 的合法性
                        // 调用 sub_52C4C0 解析并验证异常处理框架头
                        if (sub_52C4C0(a3[0], note_addr, note_addr + note_size, &v24))
                        {
                            // 验证通过！这个 SO 是合法的
                            a3[1][2] = v24[0];  // 保存验证结果
                            a3[1][3] = -1;       // 标记为成功
                            return 1;            // 返回 1 表示匹配成功
                        }
                    }
                }
            }
        }
    }
    return 0;  // 未匹配 → 这个 SO 不是目标
}
```

### 4.5 关键逻辑总结

| 步骤 | 做什么 | 通俗解释 |
|------|--------|---------|
| 1. 获取 phnum | 获取当前 SO 的程序头数量 | 看这个 SO 有多少个"段" |
| 2. 地址范围比较 | 检查目标地址是否 < SO 基址 | 目标地址在这个 SO 的范围内吗？ |
| 3. 遍历 PT_LOAD | 遍历所有需要加载的段 | 检查目标地址落在哪个段中 |
| 4. 查找 PT_GNU_EH_FRAME | 查找异常处理框架段 | 合法 SO 必须有这个段 |
| 5. 验证 .eh_frame_hdr | 解析验证段内容 | 检查这个段的结构是否合法 |

### 4.6 汇编关键点

```asm
; dl_phdr_info 结构检查
52c3ac  LDRH W21, [X0,#0x18]             ; 读取 phnum (偏移 0x18 处)
52c3b0  CBZ X21, loc_52C3CC              ; 如果 phnum == 0，跳过这个 SO
52c3b4  LDR X8, [X2,#0x10]               ; 读取目标地址
52c3b8  LDR X22, [X0]                     ; 读取 SO 基址
52c3c4  CMP X8, X22                       ; 比较: 目标地址 < 基址?

; 遍历程序头（每个程序头 56 = 0x38 字节）
52c40c  LDR W12, [X10],#0x38             ; 读取 phdr type, 指针后移 56 字节
52c410  CMP W12, #1                       ; 检查是否是 PT_LOAD (type = 1)
52c414  B.NE loop                         ; 不是 → 继续循环

; 地址范围检查
52c418  LDR X12, [X11,#0x10]             ; 读取 p_vaddr (段虚拟地址)
52c41c  ADD X12, X12, X22                 ; 加上基址，得到实际内存地址
52c424  B.CC ...                          ; 检查: 目标 < 段起始?
52c428  LDR X11, [X11,#0x28]             ; 读取 p_memsz (段内存大小)
52c42c  ADD X13, X11, X12                ; 计算段结束地址
52c434  B.CS ...                          ; 检查: 目标 >= 段结束?

; 查找 PT_GNU_EH_FRAME 段 (类型 0x6474E550)
52c440  MOV W25, #0xE550                  ; 低 16 位
52c44c  MOVK W25, #0x6474,LSL#16         ; 组合成 0x6474E550
52c46c  LDR X9, [X20,#0x10]              ; 加载 phdr 表
52c474  LDR W10, [X9,X10]                ; 读取 phdr type
52c478  CMP W10, W25                      ; 匹配?
```

---

## 5. .eh_frame_hdr 验证 sub_52C4C0

### 5.1 什么是 .eh_frame_hdr？

`.eh_frame_hdr` 是 ELF 文件中的一个特殊段，用于**异常处理**（Exception Handling）。

> **💡 通俗解释**: 当程序发生错误（如除零、空指针访问）时，异常处理机制决定如何"优雅地"处理这些错误。`.eh_frame_hdr` 就像是这个机制的"索引目录"，告诉程序哪里可以找到错误处理代码。  
> **关键点**: 合法的、由编译器生成的 SO 都包含这个段，但**注入的恶意 SO 可能没有这个段**。

### 5.2 函数原型

```c
__int64 __fastcall sub_52C4C0(
    __int64 a1,                 // 上下文
    unsigned __int8 *a2,        // .eh_frame_hdr 段的起始地址
    unsigned __int8 *a3,        // .eh_frame_hdr 段的结束地址
    __int64 a4)                 // 输出结构体
```

**地址**: 0x52C4C0  
**大小**: 0x120 字节 (288 字节)

### 5.3 伪代码（含详细注释）

```c
__int64 __fastcall sub_52C4C0(
    __int64 a1,
    unsigned __int8 *a2,        // .eh_frame_hdr 起始
    unsigned __int8 *a3,        // .eh_frame_hdr 结束
    __int64 a4)                 // 输出
{
    // 第一步: 检查长度是否足够
    // .eh_frame_hdr 至少需要 4 字节
    if (a3 - a2 <= 3)
    {
        if (a3 == a2) return 0;  // 长度为 0，肯定不合法
        
        // 长度不足，输出调试信息
        fprintf(stderr, 
            "libunwind: unsupported .eh_frame_hdr at %lx: ...\n");
        fflush(stderr);
        return 0;  // 验证失败
    }

    // 第二步: 检查版本号
    // 版本号必须为 1，这是目前唯一合法的版本
    if (*a2 != 1)
    {
        fprintf(stderr, 
            "libunwind: unsupported .eh_frame_hdr version: %u\n");
        fflush(stderr);
        return 0;  // 验证失败
    }

    // ★ 第三步: 解析 .eh_frame_hdr 头部
    // .eh_frame_hdr 的格式:
    //   [0] version: 1 字节 — 版本号 (必须为 1)
    //   [1] eh_frame_ptr_enc: 1 字节 — eh_frame 指针的编码方式
    //   [2] fde_count_enc: 1 字节 — FDE 数量的编码方式
    //   [3] table_enc: 1 字节 — 查找表项的编码方式
    //   [4...] 编码后的数据
    
    int v6 = a2[2];             // fde_count_enc — FDE 数量的编码方式
    char v7 = a2[1];            // eh_frame_ptr_enc — eh_frame 指针的编码方式
    unsigned __int8 *v13 = a2 + 4;  // 数据起始位置（跳过 4 字节头部）

    a4[24] = a2[3];             // 保存 table_enc
    
    // 解码 eh_frame 指针
    // sub_52B1A8 负责根据编码方式解码指针
    *(QWORD *)a4 = sub_52B1A8(a1, &v13, a3, v7, a2);

    // 解码 FDE 计数
    if (v6 == 0xFF)
        *(QWORD *)(a4 + 8) = 0;  // 0xFF 表示没有 FDE
    else
        *(QWORD *)(a4 + 8) = sub_52B1A8(a1, &v13, a3, v6, a2);

    *(QWORD *)(a4 + 16) = v13;  // 保存数据末尾位置

    return 1;  // 验证成功！
}
```

### 5.4 关键点

| 检查项 | 要求 | 为什么？ |
|--------|------|---------|
| 长度 >= 4 | 至少 4 字节 | 头部本身就占 4 字节 |
| 版本号 == 1 | 必须为 1 | 只有版本 1 是合法的 |
| 编码指针有效 | 能正确解码 | 编码必须符合 DW_EH_PE 规范 |

> **💡 DW_EH_PE 是什么?**  
> DW_EH_PE (DWARF Exception Header Pointer Encoding) 是一种指针编码标准，定义如何编码和解码异常处理信息中的指针。合法的编译器会正确生成这些编码，而恶意注入的 SO 可能不会。

---

## 6. 地址范围缓存数据库

### 6.1 什么是地址范围缓存？

这是一个**白名单数据库**，记录了已知的、合法的代码地址范围。当 `dl_iterate_phdr` 无法确定目标地址是否合法时，TSS 会在这个数据库中查找。

> **💡 类比**: 就像小区保安的"业主名册"，上面记录了所有合法住户的房间号。如果有人敲门，对照名册一查就知道是不是业主。

### 6.2 数据结构

```c
// 全局指针数组
__int64 *off_583328[];      // 0x583328 — 指向缓存数组的指针
__int64 *off_583330;        // 0x583330 — 缓存数组的结束指针
__int64 *off_583338;        // 0x583338 — 缓存数组的容量上限指针

// 每个缓存条目的结构 (32 字节)
// 就像名册上的一个条目
struct CacheEntry {
    __int64 id;              // [0]  — 模块 ID（标识是哪个 SO 或模块）
    __int64 addr_start;      // [1]  — 地址范围起始
    __int64 addr_end;        // [2]  — 地址范围结束
    unsigned int *data;      // [3]  — 指向编码数据的指针
};
```

### 6.3 缓存管理

在 `sub_52C0B8` 中，当检测到新的合法地址范围时，会将其添加到缓存中：

```c
// 加写锁（防止其他线程同时修改）
pthread_rwlock_wrlock(&stru_59CEFC);

// 检查缓存是否已满，需要扩容
if (off_583330 >= off_583338)  // 当前指针 >= 容量上限
{
    // 重新分配更大的缓存空间
    new_buf = malloc(old_size * 2);  // 分配两倍大小
    memcpy(new_buf, old_buf, old_size);  // 复制旧数据
    free(old_buf);  // 释放旧空间
    off_583328 = new_buf;
    off_583338 = new_buf + new_capacity;
}

// 添加新条目
entry->id = module_id;
entry->addr_start = addr;
entry->addr_end = addr + size;
entry->data = data_ptr;

// 更新结束指针
off_583330 = entry + 1;

// 解写锁
pthread_rwlock_unlock(&stru_59CEFC);
```

### 6.4 关键操作

| 操作 | 使用的锁 | 说明 |
|------|---------|------|
| 查找 | 读锁 (`rdlock`) | 只读不写，允许多线程同时查找 |
| 添加 | 写锁 (`wrlock`) | 需要修改，只允许一个线程操作 |

> **💡 什么是读写锁?**  
> 读写锁 (pthread_rwlock_t) 是一种同步机制：
> - **读锁**: 多个线程可以同时获取读锁（大家一起查）
> - **写锁**: 只有获取写锁的线程可以修改数据（一个一个改）
> 这样可以提高并发性能。

---

## 7. process_vm_readv 后备检测

### 7.1 什么是 process_vm_readv？

`process_vm_readv` 是 Linux 提供的一个**系统调用**（编号 270），允许一个进程读取另一个进程的内存。

> **💡 通俗解释**: 就像你可以通过一个"窥视镜"看到隔壁房间的情况。这里 TSS 用它来看自己的内存中某个地址存放的是什么指令。

### 7.2 系统调用细节

当 dl_iterate_phdr 枚举和地址范围缓存都未能匹配时，函数使用这个后备方案：

```c
// ★ 系统调用号 270 = process_vm_readv (ARM64 Linux)
// 原型: syscall(270, pid, local_iov, local_iovcnt, remote_iov, remote_iovcnt, flags)

// 准备 iovec 结构（用于内存读写）
// iovec 是一个"缓冲区描述符"，告诉系统从哪读、读多少
struct iovec local_iov = {
    .iov_base = &result_buffer,  // 本地缓冲区地址（数据读到这里）
    .iov_len = 8                 // 读取 8 字节
};

struct iovec remote_iov = {
    .iov_base = target_addr,     // 目标地址（从哪里读）
    .iov_len = 8                 // 读取 8 字节
};

// 执行系统调用
ssize_t ret = syscall(
    270,                        // 系统调用号
    getpid(),                   // 进程 ID（读自己的内存）
    &local_iov, 1,              // 本地缓冲区数组
    &remote_iov, 1,             // 远程地址数组
    0                           // 标志位
);
```

### 7.3 指令模式验证

```c
// 验证读取结果
if (ret == 8)  // 成功读取了 8 字节
{
    // 检查是否为 ARM64 系统调用指令序列
    // 这个 8 字节的值由两个 ARM64 指令组成:
    //
    // 0xD2801168 = MOV X8, #0x8B
    //   含义: 将立即数 0x8B (= 139) 加载到 X8 寄存器
    //   作用: 设置系统调用号
    //
    // 0xD4000001 = SVC #0x0
    //   含义: 触发超级调用 (Supervisor Call)
    //   作用: 进入内核态执行系统调用
    //
    // 这是编译器生成的典型系统调用序列
    // 合法 SO 中经常出现这种模式
    if (result_buffer == 0xD4000001D2801168LL)
    {
        // ✅ 匹配成功 → 这是合法代码
        a1->detected = 1;           // 设置检测完成标志
        a1->start_addr = addr;       // 保存起始地址
        a1->end_addr = addr + 4;     // 保存结束地址
    }
    else
    {
        // ❌ 不匹配 → 检测到注入!
        // 目标地址的代码不是预期的系统调用序列
        a1->injection_detected = 1;
    }
}
```

### 7.4 指令模式分析

```
读取的 8 字节数据被解释为两个 ARM64 指令:

┌─────────────────────────────────────────────────────────────────┐
│ 字节序: 小端 (Little-Endian)                                    │
│                                                                 │
│ 0xD2801168  →  二进制: 1101 0010 1000 0000 0001 0001 0110 1000 │
│                     MOV X8, #0x8B                                │
│                     将值 0x8B (139) 存入 X8 寄存器               │
│                                                                 │
│ 0xD4000001  →  二进制: 1101 0100 0000 0000 0000 0000 0000 0001 │
│                     SVC #0x0                                     │
│                     触发系统调用                                  │
└─────────────────────────────────────────────────────────────────┘
```

> **💡 为什么选择这个模式?**  
> 在 ARM64 Linux 中，系统调用的标准方式是：
> 1. 将系统调用号存入 X8 寄存器 (`MOV X8, #N`)
> 2. 执行 SVC 指令 (`SVC #0`)
> 
> 合法的 SO 中经常出现这种序列，而注入的代码往往不包含这种模式，或者模式不同。

---

## 8. 辅助函数分析

### 8.1 sub_5291C0 — 数据解码器

**地址**: 0x5291C0  
**大小**: 0x190 字节 (400 字节)

```c
__int64 __fastcall sub_5291C0(
    __int64 a1,         // 上下文
    unsigned int *a2,   // 编码后的数据（需要解码）
    __int64 *a3,        // 解码输出（解码后的数据）
    __int64 a4,         // 输出缓冲区
    int a5)             // 标志
```

**作用**: 解码地址范围缓存中存储的编码数据。这可能是经过 XOR 或其他简单混淆算法处理的数据，以防止被轻易分析。

> **💡 为什么需要解码?**  
> 安全软件通常会对关键数据进行混淆/编码，增加逆向分析的难度。

### 8.2 sub_52C5E0 — 二进制比较器

**地址**: 0x52C5E0

```c
__int64 __fastcall sub_52C5E0(
    __int64 a1,         // 上下文
    __int64 a2,         // 要检查的地址
    unsigned __int8 *a3,// 参考数据（预期值）
    __int64 a4,         // 参考数据大小
    unsigned __int8 *a5,// 比较数据（实际值）
    __int64 *a6,        // 输出
    __int64 a7)         // 输出
```

**作用**: 比较内存中的数据和预期的参考数据，判断是否匹配。

### 8.3 sub_52C7A8 — 模式匹配器

**地址**: 0x52C7A8

```c
__int64 __fastcall sub_52C7A8(
    __int64 a1,         // 上下文
    __int64 a2,         // 要检查的地址
    unsigned __int8 *a3,// 要匹配的模式
    int a4,             // 模式长度
    __int64 *a5,        // 输出
    __int64 a6)         // 输出
```

**作用**: 在内存中搜索特定的字节模式，用于检测已知的恶意代码特征。

### 8.4 sub_52A21C — DWARF CFI 解码器

**地址**: 0x52A21C  
**大小**: 0xC48 字节 (3144 字节) — 这是一个非常大的函数！  
**复杂度**: 包含 48 个 case 的 switch 语句

> **💡 什么是 DWARF CFI?**  
> DWARF 是一种调试信息格式，CFI (Call Frame Information) 是其中的一部分，描述了函数调用栈的结构。  
> **通俗解释**: 就像一本"建筑图纸"，描述了程序运行时"栈"的结构，帮助程序在出错时知道如何回溯。

该函数是一个完整的 DWARF Call Frame Information 解码器，支持 48 种 DW_CFA 操作码：

```c
// 支持的 DW_CFA 操作码（部分列表）
switch (opcode) {
    case 0:  // DW_CFA_advance_loc — 前进位置
    case 1:  // DW_CFA_offset — 设置偏移
    case 2:  // DW_CFA_restore — 恢复规则
    case 3:  // DW_CFA_set_loc — 设置位置
    case 4:  // DW_CFA_advance_loc1 — 前进1字节
    case 5:  // DW_CFA_advance_loc2 — 前进2字节
    case 6:  // DW_CFA_advance_loc4 — 前进4字节
    case 7:  // DW_CFA_offset_extended — 扩展偏移
    case 8:  // DW_CFA_restore_extended — 扩展恢复
    case 9:  // DW_CFA_undefined — 标记为未定义
    case 10: // DW_CFA_same_value — 值不变
    case 11: // DW_CFA_register — 使用寄存器
    case 12: // DW_CFA_remember_state — 保存状态
    case 13: // DW_CFA_restore_state — 恢复状态
    case 14: // DW_CFA_def_cfa — 定义 CFA
    case 15: // DW_CFA_def_cfa_register — 定义 CFA 寄存器
    case 16: // DW_CFA_def_cfa_offset — 定义 CFA 偏移
    // ... 还有更多
}
```

### 8.5 sub_52B1A8 — 编码指针解码器

**地址**: 0x52B1A8

```c
__int64 __fastcall sub_52B1A8(
    __int64 a1,             // 上下文
    unsigned __int8 **a2,   // 输入数据指针（会被更新）
    unsigned __int8 *a3,    // 数据结束位置
    int a4,                 // 编码格式 (DW_EH_PE)
    __int64 a5)             // 基址
```

**作用**: 解码 DW_EH_PE 编码的指针。DW_EH_PE 是一种指针编码规范，支持多种编码方式：

| 编码值 | 名称 | 说明 |
|--------|------|------|
| 0x00 | DW_EH_PE_absptr | 绝对指针 |
| 0x01 | DW_EH_PE_uleb128 | 无符号 LEB128 编码 |
| 0x02 | DW_EH_PE_udata2 | 2 字节无符号 |
| 0x03 | DW_EH_PE_udata4 | 4 字节无符号 |
| 0x04 | DW_EH_PE_udata8 | 8 字节无符号 |
| 0x09 | DW_EH_PE_sleb128 | 有符号 LEB128 编码 |
| 0x0B | DW_EH_PE_sdata4 | 4 字节有符号 |
| 0x10 | DW_EH_PE_pcrel | 相对于 PC |
| 0x30 | DW_EH_PE_textrel | 相对于文本段 |

> **💡 为什么要用不同的编码?**  
> 不同的编码方式可以节省空间或支持重定位。例如，`pcrel` 编码使用相对于当前指令的偏移量，使得代码在内存中移动时不需要修改指针值。

---

## 9. 检测流程图

```
                                 ┌──────────────────────┐
                                 │  获取目标地址 (PC)    │
                                 │  a1+0x26A = 0       │
                                 └──────────┬───────────┘
                                            │
                                            ▼
                                 ┌──────────────────────┐
                                 │ 清零"已检测"标志位   │
                                 │ a1+0x26A = 0         │
                                 └──────────┬───────────┘
                                            │
                                            ▼
         ╔══════════════════════════════════════════════════════╗
         ║   ★ 关卡一: dl_iterate_phdr 枚举所有已加载 SO     ║
         ║                                                     ║
         ║   for each loaded SO:                              ║
         ║     1. 检查地址是否在 SO 的 PT_LOAD 段内           ║
         ║     2. 查找 PT_GNU_EH_FRAME 段                     ║
         ║     3. 验证 .eh_frame_hdr 结构是否合法              ║
         ╚══════════════════════════════╤══════════════════════╝
                                        │
                    ┌───────────────────┴───────────────────┐
                    │ 成功?                                 │
                    │ (dl_iterate_phdr 返回 1 且验证通过)    │
                    └───────┬───────────────┬───────────────┘
                            │ 否            │ 是
                            │               │
                            ▼               ▼
         ╔══════════════════════╗    ╔══════════════════════════╗
         ║ 关卡二: 地址缓存     ║    ║ 返回成功结果             ║
         ║ 查询白名单数据库     ║    ║ 填充结果结构体           ║
         ║ off_583328 缓存      ║    ║ 设置 a1+0x26A = 1       ║
         ║                      ║    ╚══════════════════════════╝
         ║ 遍历缓存条目:        ║
         ║ addr_start <= addr   ║
         ║   < addr_end         ║
         ╚═══════════╤══════════╝
                     │
             ┌───────┴───────────────────┐
             │ 找到匹配?                  │
             │ (地址在缓存范围内)          │
             └───────┬───────────┬───────┘
                     │ 否        │ 是
                     │           │
                     ▼           ▼
         ╔══════════════════════╗    ╔══════════════════════════════╗
         ║ 关卡三: 后备检测     ║    ║ 解码数据                    ║
         ║ process_vm_readv     ║    ║ → sub_5291C0 解码          ║
         ║                      ║    ║ → sub_52A21C DWARF 解码    ║
         ║ syscall(270, ...)    ║    ║ → 返回成功                  ║
         ║ 读取目标地址的 8 字节 ║    ╚══════════════════════════════╝
         ║                      ║
         ║ 检查指令模式:        ║
         ║ 0xD4000001D2801168   ║
         ╚═══════════╤══════════╝
                     │
             ┌───────┴───────────────────────┐
             │ 匹配?                         │
             │ (读取的 8 字节是否等于预期值)  │
             └───────┬───────────────┬───────┘
                     │ 否            │ 是
                     │               │
                     ▼               ▼
             ╔═══════════════════╗    ╔══════════════════════════╗
             ║ ★ 检测到注入!     ║    ║ 合法代码                 ║
             ║                   ║    ║                          ║
             ║ a1+0x268 = 1     ║    ║ a1+0x26A = 1             ║
             ║ 注入检测标志      ║    ║ 检测完成标志              ║
             ╚═══════════════════╝    ╚══════════════════════════╝
```

---

## 10. 绕过思路分析

### 10.1 检测机制弱点

| 弱点 | 说明 | 难度 |
|------|------|------|
| `.eh_frame_hdr` 依赖 | 如果注入的 SO 也包含合法的 `.eh_frame_hdr`，可能绕过关卡一 | 中 |
| 缓存可被污染 | 如果能在缓存中添加非法条目，可能绕过关卡二 | 高 |
| 指令模式固定 | 只检查一个固定的 8 字节模式，其他合法指令序列不会被识别 | 低 |
| 单次检测 | 检测只在特定时刻执行，注入可以在检测完成后进行 | 中 |

### 10.2 可能的绕过方法

| 方法 | 难度 | 详细说明 |
|------|------|---------|
| **在合法 SO 中执行代码** | 🔴 高 | 不注入新的 SO，而是将代码写入已加载的合法 SO 中 |
| **Hook dl_iterate_phdr** | 🔴 高 | 拦截 `dl_iterate_phdr` 函数，过滤掉注入的 SO |
| **修改地址范围缓存** | 🔴 高 | 通过内存修改将注入 SO 的地址添加到 `off_583328` 缓存 |
| **模拟指令模式** | 🟡 中 | 在注入代码的开头放置 `0xD4000001D2801168` 这个模式 |
| **绕过 process_vm_readv** | 🟡 中 | 使用 `ptrace` 拦截系统调用，或 MMU 重定向 |
| **Patch 条件分支** | 🟢 低 | 直接修改二进制，将条件判断跳转改掉 |
| **时序攻击** | 🟡 中 | 在检测函数执行完毕后立即注入 |

### 10.3 防御建议

| 防御措施 | 说明 |
|---------|------|
| 动态检测 | 不只在固定时间检测，而是在后台持续检测 |
| 多模式匹配 | 不只检查一个固定的指令模式，而是检查多种模式 |
| 完整性校验 | 对检测代码本身进行完整性校验，防止被 Patch |
| 多路径检测 | 在不同位置使用不同的检测方法，增加绕过难度 |

---

## 11. 全局数据与关键常量

### 11.1 全局变量

| 符号 | 地址 | 描述 |
|------|------|------|
| `off_53AA20` | 0x53AA20 | vtable 函数表 — 类似"工具箱"，存放各种工具函数 |
| `unk_59CEF8` | 0x59CEF8 | 内部数据对象 — IDA 未知具体类型的数据 |
| `off_583328` | 0x583328 | 地址范围缓存数组指针 — 指向"白名单"数据库 |
| `off_583330` | 0x583330 | 缓存数组结束指针 — 数据库的末尾 |
| `off_583338` | 0x583338 | 缓存数组容量指针 — 数据库的最大容量 |
| `stru_59CEFC` | 0x59CEFC | 读写锁 (pthread_rwlock_t) — 保护缓存的并发访问 |
| `qword_59CF38` | 0x59CF38 | 缓存初始指针 — 数据库的初始位置 |
| `qword_583880` | 0x583880 | 回调函数指针 — 用于接收反作弊数据 |

### 11.2 关键常量

| 值 | 十进制 | 描述 |
|----|--------|------|
| `0x10E` | 270 | `process_vm_readv` 系统调用号 |
| `0xD4000001D2801168` | — | ARM64 系统调用指令序列 (MOV + SVC) |
| `0x6474E550` | 1685382480 | PT_GNU_EH_FRAME 段类型 |
| `0xFFFFFFFF` | -1 | 获取当前 PC 的请求参数 |
| `0xFFFFFFFE` | -2 | 获取基址的请求参数 |
| `0x3000000` | 50331648 | 结果标志（表示成功） |
| `0x618` | 1560 | 内部缓冲区大小 |
| `0x110` | 272 | 输入数据大小 |
| `0x38` | 56 | 每个程序头的大小 |

### 11.3 导入的函数

> **💡 什么是导入函数?**  
> 导入函数是指当前 SO 从其他库（如 libc.so）中调用的函数。通过分析导入函数，可以了解 SO 的功能。

| 函数 | 地址 | 用途 | 通俗解释 |
|------|------|------|---------|
| `dl_iterate_phdr` | 0x52DB00 | 枚举所有已加载的共享库 | 📋 查户口 |
| `dladdr` | 0x52D3A0 | 查询地址属于哪个 SO | 🔍 查归属 |
| `syscall` | 0x52D6C0 | 执行系统调用 | 🛠️ 调用内核 |
| `getpid` | 0x52D180 | 获取当前进程 ID | 🆔 问名字 |
| `pthread_rwlock_rdlock` | 0x52D890 | 获取读锁 | 🔒 拿钥匙（读） |
| `pthread_rwlock_wrlock` | 0x52D8A0 | 获取写锁 | 🔒 拿钥匙（写） |
| `pthread_rwlock_unlock` | 0x52D8B0 | 释放锁 | 🔓 还钥匙 |
| `malloc` | 0x52D540 | 分配内存 | 🏗️ 盖房子 |
| `free` | 0x52D4B0 | 释放内存 | 🏗️ 拆房子 |
| `memcpy` | 0x52D550 | 复制内存数据 | 📋 抄写 |
| `memset` | 0x52D400 | 设置内存数据 | 🧹 清空 |
| `fprintf` | 0x52D890 | 格式化输出（调试用） | 📝 写日志 |
| `fflush` | 0x52D910 | 刷新输出缓冲区 | 📝 提交日志 |
| `open` | 0x52D2A0 | 打开文件 | 📂 打开文件 |
| `read` | 0x52D2B0 | 读取文件 | 📖 读文件 |
| `strstr` | 0x52D060 | 字符串查找 | 🔍 找字符串 |
| `strcmp` | 0x52D810 | 字符串比较 | 🔍 比字符串 |
| `mmap` | 0x52D140 | 内存映射 | 🗺️ 映射内存 |

---

## 附录

### A. 术语表

| 术语 | 英文 | 通俗解释 |
|------|------|---------|
| **SO** | Shared Object | Android/Linux 的动态链接库，类似 Windows 的 DLL |
| **注入** | Injection | 将恶意代码强制加载到目标进程中的攻击方式 |
| **汇编** | Assembly | CPU 直接执行的机器指令的文本形式 |
| **伪代码** | Pseudocode | 由反编译器生成的类 C 代码，便于阅读 |
| **反编译** | Decompile | 将二进制机器码转换为高级语言（如 C 语言） |
| **上下文** | Context | 函数执行过程中需要的数据集合 |
| **回调** | Callback | 传给另一个函数的函数，由那个函数在适当时机调用 |
| **系统调用** | Syscall | 用户程序请求内核服务的接口 |
| **基址** | Base Address | SO 加载到内存时的起始地址 |
| **程序头** | Program Header | ELF 文件中描述段的信息结构 |
| **段** | Segment | 加载到内存中的一块连续区域（代码段、数据段等） |
| **vtable** | Virtual Table | 函数指针表，用于实现多态调用 |
| **读写锁** | RWLock | 允许多个读线程或一个写线程访问的同步机制 |
| **DWARF** | DWARF | 调试信息格式标准 |
| **CFI** | Call Frame Information | 描述函数调用栈结构的信息 |
| **LEB128** | Little Endian Base 128 | 一种变长整数编码格式 |
| **PAC** | Pointer Authentication | ARMv8.3 的安全特性，防止指针被篡改 |

### B. 关键函数地址速查表

| 函数 | 地址 | 文件偏移 | 主要作用 |
|------|------|---------|---------|
| sub_528AB0 | 0x528AB0 | 0x528AB0 | 上层调用者 1 |
| sub_528BA8 | 0x528BA8 | 0x528BA8 | 上层调用者 2 |
| sub_528D60 | 0x528D60 | 0x528D60 | 上层调用者 3 |
| sub_528F20 | 0x528F20 | 0x528F20 | 初始化检测上下文 |
| sub_529044 | 0x529044 | 0x529044 | 定位/跳转函数 |
| sub_5291C0 | 0x5291C0 | 0x5291C0 | 数据解码器 |
| **sub_5299E8** | **0x5299E8** | **0x5299E8** | **★ 核心检测函数** |
| sub_52A21C | 0x52A21C | 0x52A21C | DWARF CFI 解码器 |
| sub_52B1A8 | 0x52B1A8 | 0x52B1A8 | 编码指针解码器 |
| **sub_52C0B8** | **0x52C0B8** | **0x52C0B8** | **地址验证 + 缓存管理** |
| **sub_52C38C** | **0x52C38C** | **0x52C38C** | **dl_iterate_phdr 回调** |
| sub_52C4C0 | 0x52C4C0 | 0x52C4C0 | .eh_frame_hdr 验证 |
| sub_52C5E0 | 0x52C5E0 | 0x52C5E0 | 二进制比较 |
| sub_52C7A8 | 0x52C7A8 | 0x52C7A8 | 模式匹配 |

### C. 结果结构体定义 (C 语言)

```c
// 检测结果结构体
// 总大小: 约 0x26C 字节
struct DetectionResult {
    // 检测结果
    uint64_t start_addr;         // +0x220 (544) — 起始地址
    uint64_t end_addr;           // +0x228 (552) — 结束地址
    uint64_t data_ptr;           // +0x230 (560) — 数据指针
    uint64_t size;               // +0x238 (568) — 大小
    uint64_t aligned_addr;       // +0x240 (576) — 对齐后的地址
    uint64_t reserved;           // +0x248 (584) — 预留字段
    uint32_t flags;              // +0x250 (592) — 标志 (0x3000000)
    uint32_t type;               // +0x254 (596) — 类型
    uint64_t code_addr;          // +0x258 (600) — 代码地址
    uint64_t data_addr;          // +0x260 (608) — 数据地址
    
    // 检测标志
    uint8_t  injection_detected; // +0x268 (616) — ★ 注入检测标志 (1=检测到注入)
    uint8_t  detected;           // +0x26A (618) — ★ 检测完成标志 (1=检测通过)
};
```

### D. .c 文件中的相关代码位置

```c
// libtersafe.so.c 中的相关函数位置:
//
// sub_528F20:    第 731638 行  — 初始化上下文
// sub_5299E8:    第 732194 行  — ★ 核心检测函数
// sub_52C38C:    第 734544 行  — dl_iterate_phdr 回调
// sub_52C0B8:    第 732700 行  — 地址验证
// sub_52C4C0:    第 734980 行  — .eh_frame_hdr 验证
// sub_52A21C:    第 733000 行  — DWARF CFI 解码器
```

---

*分析完成于 2026-08-05*  
*分析工具: IDA Pro 9.0 + IDA-MCP*  
*目标: libtersafe.so (腾讯 TSS 安全 SDK)*  
*文件: E:\我的程序\ida分析2\libtersafe.so*

---

> **💡 总结**: 这个 SO 注入检测机制通过三道关卡层层筛查：
> 1. **查户口** (`dl_iterate_phdr`) — 遍历所有已加载的 SO，检查每个 SO 是否"证件齐全"（有合法的 `.eh_frame_hdr`）
> 2. **查档案** (地址缓存) — 在白名单数据库中查找目标地址
> 3. **验指纹** (`process_vm_readv`) — 直接读取内存中的指令，验证是否为合法的系统调用序列
>
> 三道关卡任意一道通过，代码就被认为是合法的；全部失败则标记为"检测到注入"。
