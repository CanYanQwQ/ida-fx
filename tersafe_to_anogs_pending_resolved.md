# 待确认偏移的详细分析报告

> 对上批文档中标记为"待确认"的偏移进行深入分析
> 分析日期: 2026-08-05

---

## 1. 0x1F9660 — sub_1F9660 (心跳/定时检测函数)

### 基本信息

| 属性 | tersafe | anogs |
|:---|:---:|:---:|
| 地址 | **0x1F9660** | **0x1EE110** |
| 函数名 | sub_1F9660 | sub_1EE110 |
| 函数签名 | `__int64 __fastcall(__int64 a1, __int64 a2, unsigned int a3, int a4)` | 相同 |

### 函数体对比

两个函数体结构**几乎相同**，只是内部调用的子函数地址不同：

| 功能 | tersafe 调用 | anogs 调用 |
|:---|:---:|:---:|
| 字符串加载 | sub_2003A0 | sub_1F4D18 |
| 全局检测 | sub_2DAD88 → sub_239CA0 | sub_2CD6E0 → sub_22EFAC |
| 初始化 | sub_1F8F3C / sub_1F8FD0 | sub_1ED75C / sub_1ED7F0 |
| 条件检测 | sub_1F9614 | sub_1EE0C4 |
| CRC 操作 | sub_4CE3AC | sub_4C0F2C |
| 心跳处理 | sub_1FB5E8 / sub_1FB7F8 | sub_1F00BC / sub_1F02C8 |
| 加解密 | sub_1FE68C | sub_1F2DC0 |
| 时间操作 | sub_22D7C0 / sub_22DA58 | sub_2225F8 / sub_22288C |
| 其他操作 | sub_1F99B8 / sub_1F9A88 / sub_1FA3C8 | sub_1EE468 / sub_1EE540 / sub_1EEE84 |

### 结论

**✅ 已确认：0x1F9660 (tersafe) → 0x1EE110 (anogs)**

---

## 2. 地址偏移量分析

以下 4 个地址在 tersafe 中**不是函数起始地址**，而是函数内部地址或数据区域。通过找到对应的函数再计算偏移量，可以得到 anogs 中的等效地址。

### 2.1 0x323390

#### 分析

| 属性 | 值 |
|:---|:---:|
| 所属函数 | sub_32337C (起始: 0x32337C) |
| 偏移量 | 0x323390 - 0x32337C = **0x14** (20 字节) |
| 函数体 | 条件跳转 `(int)a2 <= 0 ? v3=4 : v3=0` |

#### 等效函数

| 文件 | 函数 | 地址 |
|:---|:---:|:---:|
| **tersafe** | sub_32337C | 0x32337C |
| **anogs** | sub_3CE000 | 0x3CE000 |

两个函数体**完全一致**：
```c
__int64 __fastcall sub_XXX(__int64 a1, __int64 a2)
{
    __int64 v2; // x9
    unsigned int v3; // w12
    if ( (int)a2 <= 0 )
        v3 = 4;
    else
        v3 = 0;
    return (*(__int64 (__fastcall **)(__int64, __int64))(v2 + 8LL * v3))(a1, a2);
}
```

#### 计算结果

| 文件 | 地址 | 计算方式 |
|:---:|:---:|:---|
| **tersafe** | **0x323390** | 0x32337C + 0x14 |
| **anogs** | **0x3CE014** | 0x3CE000 + 0x14 |

---

### 2.2 0x325B84

#### 分析

| 属性 | 值 |
|:---|:---:|
| 所属函数 | sub_325B48 (起始: 0x325B48) |
| 偏移量 | 0x325B84 - 0x325B48 = **0x3C** (60 字节) |
| 函数体 | 条件跳转 `a2 ? v2=4 : v2=1` |
| 函数结束 | 0x325B88 (有 `unbalanced stack` 注释) |

#### 等效函数

| 文件 | 函数 | 地址 |
|:---|:---:|:---:|
| **tersafe** | sub_325B48 | 0x325B48 |
| **anogs** | sub_3D07CC | 0x3D07CC |

两个函数体**完全一致**：
```c
void __fastcall sub_XXX(__int64 a1, __int64 a2)
{
    int v2; // w10
    if ( a2 )
        v2 = 4;
    else
        v2 = 1;
    __asm { BR              X10 }
}
```

#### 计算结果

| 文件 | 地址 | 计算方式 |
|:---:|:---:|:---|
| **tersafe** | **0x325B84** | 0x325B48 + 0x3C |
| **anogs** | **0x3D0808** | 0x3D07CC + 0x3C |

---

### 2.3 0x325BA0

#### 分析

| 属性 | 值 |
|:---|:---:|
| 所属函数 | sub_325B8C (起始: 0x325B8C) |
| 偏移量 | 0x325BA0 - 0x325B8C = **0x14** (20 字节) |
| 函数体 | 条件跳转 `a1 == 0 ? (a2+8*0) : (a2+8*1)` |

#### 等效函数

| 文件 | 函数 | 地址 |
|:---|:---:|:---:|
| **tersafe** | sub_325B8C | 0x325B8C |
| **anogs** | sub_3D0810 | 0x3D0810 |

两个函数体**完全一致**：
```c
__int64 __usercall sub_XXX@<X0>(int a1@<W2>, __int64 a2@<X8>)
{
    return (*(__int64 (__fastcall **)(_QWORD))(a2 + 8LL * (a1 == 0)))(0LL);
}
```

#### 计算结果

| 文件 | 地址 | 计算方式 |
|:---:|:---:|:---|
| **tersafe** | **0x325BA0** | 0x325B8C + 0x14 |
| **anogs** | **0x3D0824** | 0x3D0810 + 0x14 |

---

### 2.4 0x460720

#### 分析

| 属性 | 值 |
|:---|:---:|
| 所属函数 | sub_460744 (起始: 0x460744) 的**前方** |
| 偏移量 | 0x460720 = 0x460744 - **0x24** (36 字节) |
| 区域性质 | 函数前的数据区域（可能为 padding 或跳转表） |

#### 等效函数

| 文件 | 函数 | 地址 | 功能 |
|:---|:---:|:---:|:---|
| **tersafe** | sub_460744 | 0x460744 | `return a1 + 6;` |
| **anogs** | sub_354AB0 | 0x354AB0 | `return a1 + 6;` |

#### 相关函数表

sub_460744 和 sub_460760 属于同一组内联函数，它们出现在两个函数表中：

**表 1 (3 项):**
```c
// tersafe: off_517C08[3] = { sub_45F0BC, sub_45F194, sub_4607BC }
// anogs:   off_5091B8[3] = { sub_353270, sub_35336C, sub_354B28 }
```

**表 2 (4 项):**
```c
// tersafe: off_517C40[4] = { sub_45F160, sub_45F1D0, sub_460778, sub_45FFC8 }
// anogs:   off_5091F0[4] = { sub_353338, sub_3533A8, sub_354AE4, sub_3541A0 }
```

#### 计算结果

| 文件 | 地址 | 计算方式 |
|:---:|:---:|:---|
| **tersafe** | **0x460720** | 0x460744 - 0x24 |
| **anogs** | **0x354A8C** | 0x354AB0 - 0x24 |

> ⚠️ **注意**：0x460720 是函数前的数据区域，该区域在 anogs 中可能内容不同（因为编译后数据布局可能变化）。0x354A8C 是位置上的等效地址，但内容不一定相同。

---

## 3. 完整映射汇总表

| 序号 | tersafe 偏移 | 类型 | anogs 偏移 | 匹配方法 |
|:---:|:---:|:---|:---:|:---|
| 1 | **0x1F9660** | 函数 | **0x1EE110** | 函数体模式匹配 |
| 2 | **0x323390** | 函数内部 (sub_32337C+0x14) | **0x3CE014** | 函数映射 + 偏移计算 |
| 3 | **0x325B84** | 函数内部 (sub_325B48+0x3C) | **0x3D0808** | 函数映射 + 偏移计算 |
| 4 | **0x325BA0** | 函数内部 (sub_325B8C+0x14) | **0x3D0824** | 函数映射 + 偏移计算 |
| 5 | **0x460720** | 数据区域 (sub_460744-0x24) | **0x354A8C** | 函数映射 + 偏移计算 |

---

## 4. 伴随函数映射（连带发现）

在查找上述函数时，连带发现了以下函数对应关系：

### 4.1 条件跳转函数组

| 功能 | tersafe 地址 | tersafe 函数 | anogs 地址 | anogs 函数 |
|:---|:---:|:---|:---:|:---|
| 条件跳转 (a2<=0) | 0x32337C | sub_32337C | 0x3CE000 | sub_3CE000 |
| 条件跳转 (a2) | 0x325B48 | sub_325B48 | 0x3D07CC | sub_3D07CC |
| 条件跳转 (a1==0) | 0x325B8C | sub_325B8C | 0x3D0810 | sub_3D0810 |

### 4.2 内联工具函数组

| 功能 | tersafe 地址 | tersafe 函数 | anogs 地址 | anogs 函数 |
|:---|:---:|:---|:---:|:---|
| `return a1 + 6` | 0x460744 | sub_460744 | 0x354AB0 | sub_354AB0 |
| `return *(u16*)(a1+4)` | 0x460760 | sub_460760 | 0x354ACC | sub_354ACC |

### 4.3 函数表映射

| 表 | tersafe 表 | anogs 表 |
|:---|:---|:---|
| 3 项表 | `off_517C08` | `off_5091B8` |
| 4 项表 | `off_517C40` | `off_5091F0` |

**3 项表内容:**
```c
// tersafe: { &sub_45F0BC, &sub_45F194, &sub_4607BC }
// anogs:   { &sub_353270, &sub_35336C, &sub_354B28 }
```

**4 项表内容:**
```c
// tersafe: { &sub_45F160, &sub_45F1D0, &sub_460778, &sub_45FFC8 }
// anogs:   { &sub_353338, &sub_3533A8, &sub_354AE4, &sub_3541A0 }
```

---

## 5. 总结

所有待确认的偏移都已找到对应关系：

- **0x1F9660** → 完全匹配的函数 `sub_1EE110` (anogs)
- **0x323390/0x325B84/0x325BA0** → 通过找到等效函数 + 偏移量计算得出
- **0x460720** → 通过等效函数的位置偏移计算得出（注意：数据区域内容可能不同）

这些函数体在 tersafe 和 anogs 之间**完全一致**，只是地址因重编译而不同。