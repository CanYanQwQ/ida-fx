# libanogs.so 反作弊绕过补丁文档

> **目标文件**: `libanogs(反vmp).so` — ANOGS 反作弊 SDK v7.7.31.5614  
> **架构**: ARM64 (AArch64)  
> **补丁总数**: 13 个函数入口（全部用 RET 或 MOV X0,#0; RET 替换）

---

## 一、补丁总览图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      ANOGS 检测架构 + 补丁点位置                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  检测线程 (各自运行检测逻辑)                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │maps扫描  │  │ELF检测   │  │CRC校验   │  │栈回溯    │  │环境检测等     │  │
│  │ ②③⑬     │  │ ④⑤⑥     │  │ ⑫       │  │ ⑦⑧      │  │ ⑨           │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬───────┘  │
│       │             │             │             │               │          │
│       ▼             ▼             ▼             ▼               ▼          │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                    79路 Switch 分发器  ①                              │  │
│  │  → 直接 RET，不执行任何 case                                           │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                    错误上报函数  ⑩                                   │  │
│  │  → 直接 RET，不发送任何数据                                            │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ════════════════════════════════════════════════════════════════════════    │
│  以上所有检测结果 → 全部被阻断，无法到达服务器                                │
│  ════════════════════════════════════════════════════════════════════════    │
│                                                                             │
│  BSS标志位 (byte_54ABBD~54ABD3): 不需要修改                                │
│  原因: ① 已经 RET，79路Switch 不会执行任何 case，标志位永远不会被检查       │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、需要修改的内容

### 直接照着这个表改就行

| # | 地址 | 长度 | 原始字节 | 改为（要写入的字节） | 含义 |
|---|------|------|---------|-------------------|------|
| ① | `0x37966C` | 4 | `FC 0F 1E F8` | `C0 03 5F D6` | RET |
| ② | `0x373ED4` | 8 | `FC 0F 1E F8` | `00 00 80 52 C0 03 5F D6` | MOV X0,#0; RET |
| ③ | `0x3745E8` | 8 | `FC 0F 1E F8` | `00 00 80 52 C0 03 5F D6` | MOV X0,#0; RET |
| ④ | `0x375344` | 4 | `FC 0F 1E F8` | `C0 03 5F D6` | RET |
| ⑤ | `0x317954` | 4 | `FC 0F 1E F8` | `C0 03 5F D6` | RET |
| ⑥ | `0x32893C` | 4 | `FC 0F 1E F8` | `C0 03 5F D6` | RET |
| ⑦ | `0x2F4644` | 8 | `FF C3 02 D1` | `00 00 80 52 C0 03 5F D6` | MOV X0,#0; RET |
| ⑧ | `0x2E29E0` | 8 | `FC 0F 1E F8` | `00 00 80 52 C0 03 5F D6` | MOV X0,#0; RET |
| ⑨ | `0x3E8508` | 4 | `20 00 20 D4` | `C0 03 5F D6` | RET |
| ⑩ | `0x379220` | 4 | `FF 43 06 D1` | `C0 03 5F D6` | RET |
| ⑪ | `0x425864` | 4 | `EF 3B B6 6D` | `C0 03 5F D6` | RET |
| ⑫ | `0x1CF5E4` | 4 | `FF 83 02 D1` | `C0 03 5F D6` | RET |
| ⑬ | `0x37E580` | 4 | `FF C3 05 D1` | `C0 03 5F D6` | RET |

**操作方法**: 在 IDA Pro 中按 `G` 输入地址跳转，然后 `Edit → Patch Program → Change byte`，输入右侧的字节序列。或者用 `patch` 工具直接写入。

---

## 三、为什么要改这些函数

| # | 函数名 | 这个函数是干什么的 | 改了之后会怎样 |
|---|--------|------------------|--------------|
| ① | `sub_37966C` | **79路Switch分发器** — 检测结果的总调度台。所有检测线程发现异常后，都把数据送到这里，它按类型分79个case处理，每个case再调上报函数。 | 79路Switch直接返回，不处理任何检测结果。所有检测线程发现异常后无法上报。 |
| ② | `sub_373ED4` | **/proc/self/maps 读取** — 打开这个文件，逐行查看所有加载到内存中的库，检查有没有陌生库或异常权限。16处调用。 | 返回0，调用者认为"没找到异常"。 |
| ③ | `sub_3745E8` | **/proc/self/maps 解析** — 对maps每行做深度解析，提取地址范围、权限、路径名。3处调用。 | 返回0，调用者认为"无匹配"。 |
| ④ | `sub_375344` | **ELF段检测** — 检查 `.rodata`、`.rela.plt` 等段有没有被篡改（GOT/PLT Hook检测）。7处调用。 | 直接返回，不做任何检查。 |
| ⑤ | `sub_317954` | **批量ELF检测(14段)** — 一次性检查14个ELF段的状态。 | 直接返回。 |
| ⑥ | `sub_32893C` | **批量ELF检测(扩展版)** — 同上，增加偏移计算和哈希校验。 | 直接返回。 |
| ⑦ | `sub_2F4644` | **栈回溯入口** — 用libunwind遍历调用栈，检查有没有不该出现的代码（如Frida注入）。7处调用。 | 返回NULL，栈回溯功能关闭。 |
| ⑧ | `sub_2E29E0` | **libunwind初始化** — 初始化栈回溯需要的上下文。 | 返回0，libunwind无法启动。 |
| ⑨ | `sub_3E8508` | **主检测引擎** — 整个反作弊中最大的函数(38KB)，多个检测线程的核心入口。 | 直接返回，38KB检测逻辑全部跳过。 |
| ⑩ | `sub_379220` | **CRC错误上报函数** — 所有检测结果的最终上报出口。组装 `"name=xx\|desc=yy"` 格式的消息并发送。 | 直接返回，所有25+种上报静默丢弃。 |
| ⑪ | `sub_425864` | **数据上报模块** — 第二大函数(32KB)，负责序列化数据并网络发送。 | 直接返回，不上报任何数据。 |
| ⑫ | `sub_1CF5E4` | **OLLVM数据校验引擎** — 对数据包做完整性校验（OLLVM混淆，状态机跳转）。 | 直接返回，数据包校验跳过。 |
| ⑬ | `sub_37E580` | **内存段扫描** — 遍历进程内存段，匹配目标库名，检查权限。 | 直接返回，内存扫描跳过。 |

---

## 四、什么时候用 RET，什么时候用 MOV X0,#0; RET

| 字节序列 | 汇编指令 | 什么时候用 |
|---------|---------|-----------|
| `C0 03 5F D6` | `RET` | 函数是 void 类型（没有返回值），或者调用者不检查返回值 |
| `00 00 80 52 C0 03 5F D6` | `MOV X0,#0; RET` | 函数有返回值（返回指针或布尔值），调用者需要根据返回值决定下一步 |

**当前补丁中的选择**:
- 用 `RET` 的: ①④⑤⑥⑨⑩⑪⑫⑬ — 这些是 void 函数或调用者不关心返回值
- 用 `MOV X0,#0; RET` 的: ②③⑦⑧ — 这些函数返回指针/布尔值，调用者检查返回值

---

## 五、为什么不需要改 BSS 标志位

BSS 段中的 `byte_54ABBD` ~ `byte_54ABD3` 是 79路Switch 每个 case 的"一次性开关"：

```
case 0:
    if (byte_54ABBD & 1) {   // 检查标志位
        byte_54ABBD = 0;      // 清零
        sub_379220(...);      // 上报
    }
```

但 ① `sub_37966C` 已经 Patch 为 RET，79路Switch 直接返回，**根本不会执行到检查标志位的代码**。所以标志位不需要改。

---

## 六、补丁效果总结

| 检测类型 | 通过什么阻断 | 效果 |
|---------|------------|------|
| 所有检测结果的上报 | ⑩ 上报函数 → RET | 所有上报静默丢弃 |
| 所有检测结果的分发 | ① 79路Switch → RET | 79种检测结果无法处理 |
| /proc/self/maps 检测 | ②③ maps读取/解析 → 返回0 | 检测不到任何内存异常 |
| ELF 段完整性检测 | ④⑤⑥ 三个ELF函数 → RET | 检测不到段被篡改 |
| 栈回溯检测 | ⑦⑧ 栈回溯函数 → 返回0/NULL | 检测不到注入代码 |
| 主检测引擎 | ⑨ 38KB主引擎 → RET | 跳过所有核心检测逻辑 |
| 数据上报模块 | ⑪ 32KB上报模块 → RET | 无法发送任何数据 |
| OLLVM 数据校验 | ⑫ 校验引擎 → RET | 跳过数据包完整性校验 |
| 内存段扫描 | ⑬ 内存扫描 → RET | 跳过内存段扫描 |